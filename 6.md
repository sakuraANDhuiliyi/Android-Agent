# 阶段六：Conversation Event 查询 API

项目路径：`/Users/mac/Android-Agent`

请新增安全、可分页的 Conversation Event 查询 API，并验证所有旧 Conversation、Job、WebSocket 接口兼容。

## 通用要求

- 只新增查询能力，不改 UI，不重构任务系统。
- 严格执行用户隔离。
- 不暴露 API Key、Authorization 或配置密钥。
- 完成后运行全部 Python 测试。

阅读：

- `agent/api.py`
- `agent/database.py`
- `agent/conversation_events.py`
- 当前认证和用户隔离逻辑

## 新增接口

`GET /api/conversations/{conversation_id}/events`

参数：

- `after_seq`：可选整数。
- `limit`：默认 200，最小 1，最大 500。
- `context_only`：默认 false。

返回：

```json
{
  "conversation_id": "...",
  "events": [],
  "next_after_seq": 123,
  "has_more": false
}
```

要求：

- Conversation 必须属于当前用户。
- 其他用户访问返回 404，不泄露资源是否存在。
- 按 `seq` 升序。
- 只返回 `seq > after_seq`。
- `context_only=true` 仅返回 `context_visible=true`。
- `payload_json` 转换为 `payload` 对象。
- `context_visible` 返回布尔值。
- `limit` 严格限制。
- 不返回 SQLite Row、连接对象或内部异常对象。
- 分页不得重复或跳过事件。

## 旧接口兼容

确认以下接口不回归：

- `GET/POST /api/projects/{id}/conversations`
- `GET/PATCH/DELETE /api/conversations/{id}`
- `POST /api/conversations/{id}/ask`
- `POST /api/projects/{id}/ask`
- `GET /api/jobs/{id}`
- `WebSocket /api/ws/jobs/{id}`

`GET /api/conversations/{id}` 继续返回：

- `id`
- `user_id`
- `project_id`
- `title`
- `status`
- `turns`
- `turn_count`
- `created_at`
- `updated_at`

`turns` 保持原格式，不直接暴露中间工具事件。

## 测试

至少覆盖：

1. 第一页数量正确。
2. `next_after_seq` 和 `has_more` 正确。
3. 后续分页不重复、不跳过。
4. `context_only` 过滤正确。
5. 事件顺序正确。
6. `limit` 边界正确。
7. 用户不能访问他人 Conversation。
8. 不存在的 Conversation 返回 404。
9. Conversation 详情旧格式不变。
10. Job 和 WebSocket 相关测试不回归。
11. 响应中不存在凭证字段。

如接口行为需要文档说明，只做最小 README 更新。

最终回复需列出接口格式、分页规则、鉴权规则和测试结果。
