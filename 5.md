# 阶段五：审批持久化与服务中断修复

项目路径：`/Users/mac/Android-Agent`

请在规范事件已接入 Agent 循环的基础上，实现审批事件持久化和服务重启后的不完整工具链修复。

## 通用要求

- 不自动重新执行任何中断工具。
- 恢复逻辑必须幂等。
- 保持现有桌面端审批接口兼容。
- 不调用真实模型或网络。
- 完成后运行全部 Python 测试。

阅读：

- `agent/approvals.py`
- `agent/jobs.py`
- `agent/database.py`
- `agent/conversation_events.py`
- 当前 `recover_interrupted()`

## 审批事件

`approval_required` 同时写入现有 `task_event` 和规范 Conversation Event，payload 至少包含：

```json
{
  "approval_id": "...",
  "kind": "download_file",
  "tool_call_id": "...",
  "request": {},
  "message": "等待用户确认"
}
```

`approval_resolved` 至少包含：

```json
{
  "approval_id": "...",
  "kind": "download_file",
  "tool_call_id": "...",
  "decision": "approved|rejected|timeout|canceled",
  "message": "用户确认结果"
}
```

要求：

- 关联 `conversation_id`、`turn_id`、`task_id`、`tool_call_id`。
- 使用稳定 `event_key` 防止重复。
- 审批事件 `context_visible=false`。
- 审批后的工具成功或失败结果通过 `tool_result` 进入模型上下文。

## 中断修复

扩展启动恢复逻辑，对 `queued`、`running`、`awaiting_approval` 的 Turn：

1. 标记为 `interrupted`。
2. 追加 `turn_interrupted`。
3. 使用 `recovery:{turn_id}:interrupted` 等稳定 `event_key`。
4. 查找没有对应 `tool_result` 的 `tool_call`。
5. 为每个孤立调用追加合成失败结果。

合成 payload：

```json
{
  "tool_call_id": "...",
  "name": "...",
  "ok": false,
  "model_output": "工具调用因 Agent 服务中断，未完成执行。",
  "structured_output": null,
  "duration_ms": null,
  "error_type": "service_interrupted",
  "interrupted": true
}
```

要求：

- 合成结果有稳定 `event_key`。
- 重复启动不会重复追加。
- 不重新执行有副作用的工具。
- Task 状态与现有恢复行为兼容。
- 待审批请求重启后视为失效。
- 下一轮上下文不存在孤立 `tool_call`。
- 孤立 `tool_result` 不进入上下文，并留下诊断信息。

## 测试

至少覆盖：

1. 审批申请及四种决定持久化。
2. 审批事件不直接进入模型上下文。
3. 审批后的工具结果进入上下文。
4. `running` Turn 重启后成为 `interrupted`。
5. `awaiting_approval` 重启后成为 `interrupted`。
6. 孤立工具调用得到合成失败结果。
7. 恢复逻辑重复执行仍幂等。
8. 下一轮上下文没有孤立工具调用。
9. 不会自动重新执行中断工具。
10. 现有审批 API 和测试继续通过。

最终回复需说明审批事件链、恢复幂等键和不完整工具配对修复结果。
