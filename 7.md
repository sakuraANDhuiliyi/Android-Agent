# 阶段七：完整集成测试、整理与最终审计

项目路径：`/Users/mac/Android-Agent`

本阶段不新增主要功能，只补齐完整集成测试、清理重复逻辑、更新必要文档并做最终审计。

## 通用要求

- 保留所有现有功能和未提交无关改动。
- 不调用真实模型或网络。
- 不修改密钥。
- 不用异常吞噬掩盖规范事件错误。
- 完成后运行全部验证。

审查：

- `agent/database.py`
- `agent/conversation_events.py`
- `agent/conversation_context.py`
- `agent/jobs.py`
- `agent/loop.py`
- `agent/approvals.py`
- `agent/api.py`
- `tests/test_mvp_core.py`
- `tests/test_conversation_events.py`

## 行为审计

确认：

1. `conversation_events` 是新会话上下文的权威来源。
2. `turns_json` 只承担迁移与兼容职责。
3. 不再只恢复最近 6 轮最终问答。
4. 每次模型响应都有 `assistant_message`。
5. 每个工具调用都有稳定 ID。
6. 完成、失败或中断工具都有 `tool_result`。
7. 当前用户 Prompt 不重复。
8. `task_events` 继续服务 UI 流式事件。
9. `text_delta` 没有逐条写入规范事件。
10. 不保存隐藏思维链和 API Key。
11. Provider 转换不依赖真实模型。
12. 恢复与旧数据迁移均幂等。
13. Conversation 用户隔离正确。
14. 关键事件写入失败不会被静默忽略。

## Fake 端到端测试

实现完整流程：

第一轮：

1. 用户要求读取文件。
2. fake 模型返回 `read_file` 工具调用。
3. fake 工具返回内容。
4. fake 模型返回最终回答。

然后：

1. 关闭并重新打开 SQLite。
2. 启动第二轮。
3. 验证上下文包含第一轮：
   - `user_message`
   - 中间 `assistant_message`
   - `tool_call`
   - `tool_result`
   - 最终 `assistant_message`

还需覆盖：

- 工具失败。
- Provider/Model 切换。
- 服务中断恢复。
- 八轮以上历史。
- 旧数据迁移。
- API 分页和用户隔离。

## 代码整理

- OpenAI 与 Anthropic 共用事件记录器。
- 事件类型集中定义。
- JSON 编解码集中处理。
- 删除已不再使用的最近 6 轮主路径。
- 兼容函数增加清晰注释。
- 不保留两套可能产生分歧的会话写入主流程。
- 不使用 `except Exception: pass` 吞掉关键规范事件错误。

## 文档

在 README 中简要说明：

- Conversation Event 模型。
- 旧数据自动迁移。
- 事件查询 API。
- `task_events` 与 `conversation_events` 的职责区别。
- 尚未实现语义摘要、长期记忆和运行中消息队列。

## 最终验证

运行：

```bash
python3 -m unittest discover -s tests -v
```

如果修改了 JavaScript：

```bash
for f in desktop/src/*.js agent/web/app.js; do
  node --check "$f" || exit 1
done
```

再运行：

```bash
git diff --check
git status --short
```

最终回复必须包含：

1. 总体结果。
2. 新增数据结构。
3. 规范事件类型。
4. 上下文重建方式。
5. 旧数据迁移方式。
6. 中断修复方式。
7. API 变化。
8. 修改文件。
9. 测试命令和通过数量。
10. 剩余风险。
11. 下一阶段建议。

不要只回复“已完成”，必须提供测试与关键文件证据。
