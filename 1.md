# 阶段一：数据库与事件存储基础

项目路径：`/Users/mac/Android-Agent`

请直接阅读代码并完成本阶段修改，不要只输出方案或伪代码。

## 通用要求

- 只完成本阶段，不提前实现后续阶段。
- 保留工作区已有改动，不覆盖或回滚无关代码。
- 禁止使用 `git reset`、`git checkout` 等破坏性命令。
- 不修改 `config.yaml` 或任何 API Key。
- 不调用真实模型或真实网络，测试使用 mock/fake。
- 保持现有 API、`task_events`、WebSocket、桌面端和 Android 客户端兼容。
- 完成后运行 `python3 -m unittest discover -s tests -v`。

## 目标

新增 `conversation_turns`、`conversation_events` 两张表，并实现可靠、不可变、可并发追加的事件存储服务。本阶段不要接入 Agent 模型循环。

开始前完整阅读：

- `agent/database.py`
- `agent/jobs.py`
- `tests/test_mvp_core.py`

## 数据库结构

### conversation_turns

至少包含：

- `id TEXT PRIMARY KEY`
- `conversation_id TEXT NOT NULL`
- `task_id TEXT`
- `user_id TEXT NOT NULL`
- `project_id TEXT NOT NULL`
- `status TEXT NOT NULL`
- `provider TEXT`
- `model TEXT`
- `created_at REAL NOT NULL`
- `started_at REAL`
- `finished_at REAL`
- `error_message TEXT`
- `schema_version INTEGER NOT NULL DEFAULT 1`

约束：

- `conversation_id` 外键关联 `conversations(id)`，`ON DELETE CASCADE`。
- `task_id` 存在时唯一。
- 为 `conversation_id + created_at`、`task_id`、`user_id + project_id` 建立索引。
- 状态支持 `queued`、`running`、`awaiting_approval`、`succeeded`、`failed`、`canceled`、`interrupted`。

### conversation_events

至少包含：

- `id TEXT PRIMARY KEY`
- `conversation_id TEXT NOT NULL`
- `turn_id TEXT NOT NULL`
- `task_id TEXT`
- `seq INTEGER NOT NULL`
- `event_type TEXT NOT NULL`
- `role TEXT`
- `context_visible INTEGER NOT NULL DEFAULT 0`
- `provider TEXT`
- `model TEXT`
- `payload_json TEXT NOT NULL DEFAULT '{}'`
- `event_key TEXT`
- `created_at REAL NOT NULL`
- `schema_version INTEGER NOT NULL DEFAULT 1`

约束：

- `conversation_id` 外键关联 `conversations(id)`，`ON DELETE CASCADE`。
- `turn_id` 外键关联 `conversation_turns(id)`，`ON DELETE CASCADE`。
- `UNIQUE(conversation_id, seq)`。
- `UNIQUE(conversation_id, event_key)`。
- 为 `conversation_id + seq`、`turn_id + seq`、`task_id`、`event_type` 建立索引。

## 事件存储模块

建议新增 `agent/conversation_events.py`，至少提供：

- `create_turn()`
- `get_turn()`
- `get_turn_by_task()`
- `update_turn_status()`
- `append_event()`
- `append_event_idempotent()`
- `list_events()`
- `list_turn_events()`

要求：

- 事件只能追加，不提供修改历史事件的方法。
- 同一 Conversation 的 `seq` 严格递增。
- `seq` 必须通过 SQLite 事务安全分配，不能只依赖进程内计数器。
- 使用 `BEGIN IMMEDIATE`、现有锁或等价方案避免并发重复序号。
- 重复 `event_key` 返回已有事件，不重复写入。
- `payload_json` 对外转换为字典。
- JSON 序列化失败必须抛出可诊断错误。
- 不得把 API Key、Authorization、数据库连接或客户端对象写入 payload。

## 测试

新增 `tests/test_conversation_events.py`，至少覆盖：

1. 创建 Turn 和更新状态。
2. 事件序号严格递增。
3. `event_key` 幂等。
4. 数据库重开后事件仍存在。
5. Conversation 和用户隔离。
6. 两个线程向同一 Conversation 追加时不产生重复序号。
7. 外键和级联行为正确。
8. JSON 损坏或不可序列化时产生明确错误。

## 本阶段禁止

- 不修改 `agent/loop.py`。
- 不改变模型上下文。
- 不迁移 `turns_json`。
- 不新增 HTTP API。
- 不实现服务重启修复。

最终回复需列出修改文件、表结构、并发策略、测试命令和测试结果。
