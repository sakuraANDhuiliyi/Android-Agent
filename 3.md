# 阶段三：Provider 无关的上下文重建器

项目路径：`/Users/mac/Android-Agent`

请在阶段一、二已完成的基础上，实现从规范事件重建 OpenAI-compatible 和 Anthropic 消息。本阶段只实现转换与测试，不接入真实 Agent 循环。

## 通用要求

- 不调用真实模型或网络。
- 保留现有任务内 compact 逻辑，但新跨轮上下文不再依赖最近 6 轮摘要。
- 转换函数尽量保持纯函数。
- 完成后运行全部 Python 测试。

阅读：

- `agent/compact.py`
- `agent/loop.py`
- `agent/conversation_events.py`
- 现有 OpenAI-compatible、Anthropic 消息构造逻辑

建议新增 `agent/conversation_context.py`。

## 可进入模型上下文的事件

仅包含：

- `user_message`
- `assistant_message`
- `tool_call`
- `tool_result`
- `context_checkpoint`
- `context_visible=true` 的 `system_note` / `recovery_note`

排除：

- `text_delta`
- `usage`
- `turn_started` / `turn_completed`
- `changes`
- `provider_switch` / `model_switch`
- `approval_required` / `approval_resolved`
- UI preview 和遥测事件

## OpenAI-compatible 重建

生成标准 Chat Completions 工具消息：

- `user_message` → `{"role":"user","content":"..."}`
- 同一 `message_id` 的 `assistant_message` 与 `tool_call` 合并为一条 assistant 消息。
- `tool_result` → `{"role":"tool","tool_call_id":"...","content":"..."}`。

要求：

- 多个工具调用按 `block_index` 排序。
- `function.arguments` 必须是合法 JSON 字符串。
- 保留原始 `tool_call_id`。
- 工具失败结果同样进入上下文。
- 不生成孤立 `tool_result`。

## Anthropic 重建

- 用户文本构造成 `role=user`。
- Assistant 文本与工具调用合并成 `role=assistant` 的 `text`、`tool_use` blocks。
- 工具结果构造成下一条 `role=user` 的 `tool_result` blocks。
- 多个连续工具结果可以合并，但顺序必须稳定。
- `tool_use.id` 与 `tool_result.tool_use_id` 必须一致。

## 历史范围

- 不再用 `build_session_prior_messages(max_turns=6)` 作为新会话上下文来源。
- 加载全部尚未被最后一个有效 `context_checkpoint` 覆盖的规范事件。
- 本阶段不生成语义摘要。
- 数据库事件不得因模型视图裁剪而删除。
- 当前用户 Prompt 不得出现两次。

## 测试

至少覆盖：

1. OpenAI 普通多轮消息。
2. OpenAI 单个及多个工具调用。
3. OpenAI 工具失败。
4. OpenAI arguments 是合法 JSON。
5. Anthropic 普通多轮消息。
6. Anthropic `tool_use` / `tool_result`。
7. Anthropic 多工具顺序。
8. 非上下文事件被排除。
9. 工具 ID 保持一致。
10. 八轮历史中第一轮仍存在。
11. Anthropic 历史可转换到 OpenAI。
12. OpenAI 历史可转换到 Anthropic。
13. `context_checkpoint` 起点正确。
14. 当前用户输入不重复。
15. 孤立工具事件不会构造成非法消息。

本阶段不要修改任务生命周期，不新增 API，不实现服务重启恢复。

最终回复需说明两种 Provider 的重建规则、历史边界和测试结果。
