# 阶段四：将规范事件接入 Agent 运行循环

项目路径：`/Users/mac/Android-Agent`

请在前三阶段完成的基础上，把规范事件接入真实任务、模型响应和工具执行流程。

## 通用要求

- 不调用真实模型，集成测试使用 fake 模型和 fake 工具。
- 保留现有 `task_events`、WebSocket 和 UI 流式事件。
- 关键规范事件写入失败时不得静默继续。
- 完成后运行全部 Python 测试。

阅读：

- `agent/jobs.py`
- `agent/loop.py`
- `agent/stream.py`
- `agent/tools.py`
- `agent/conversation_events.py`
- `agent/conversation_context.py`

## 任务创建

调整 `start_ask_job()`：

1. 校验 Conversation。
2. 创建 task。
3. 创建 `conversation_turn`。
4. 在线程启动前立即写入 `user_message`。
5. 加载该 Conversation 的历史规范事件。
6. 启动后台线程。
7. 把 `turn_id` 传入 `_run_job()` 和 `run_agent()`。

当前 Prompt 只能进入模型一次。可以排除当前 Turn 后继续由 `run_agent` 追加，也可以完全从事件构造，但必须统一并测试。

## 模型响应

OpenAI-compatible 和 Anthropic 每完成一次模型响应：

1. 合并流式文本。
2. 生成稳定 `message_id`。
3. 写入一条 `assistant_message`。
4. 写入该响应的每个 `tool_call`。
5. 完成规范事件写入后才能执行工具。

`assistant_message` 至少记录：

- `text_blocks`
- `finish_reason`
- `is_final`
- `streamed`
- 实际 `provider` / `model`
- 可安全保存时的不透明 response ID

不得保存隐藏思维链或 API 凭证。

## 工具调用和结果

`tool_call` 至少记录：

- `message_id`
- `tool_call_id`
- `block_index`
- `name`
- `input`

`tool_result` 至少记录：

- `tool_call_id`
- `name`
- `ok`
- `model_output`
- `structured_output`
- `duration_ms`
- `error_type`
- `interrupted`

要求：

- Provider 没有可靠 ID 时生成稳定 ID，并在结果中复用。
- `model_output` 是实际传回模型的完整字符串，不是 UI preview。
- 工具失败和异常也必须形成 `tool_result`。
- `tool_call` 写入失败时不得执行工具。
- `tool_result` 写入失败时任务必须失败。

## Turn 状态

记录并同步：

- `turn_started`
- `usage`
- `provider_switch`
- `model_switch`
- `changes`
- `turn_completed`
- `turn_failed`
- `turn_canceled`

成功时 Turn 为 `succeeded`，最终 Assistant 响应标记 `is_final=true`。失败和取消时对应状态、事件必须一致。

移除 `_run_job()` 末尾把最终问答再次作为唯一历史写入的旧主流程，避免重复 Turn。

## 测试

至少覆盖：

1. 用户消息在线程启动前持久化。
2. 普通回答形成完整事件链。
3. 多次模型响应分别保存。
4. 工具调用与结果正确配对。
5. 工具失败被保存并传回模型。
6. 当前 Prompt 不重复。
7. 第二轮能收到第一轮工具调用和结果。
8. Provider/Model fallback 记录实际值。
9. `task_events` 继续正常产生。
10. 任务成功、失败、取消与 Turn 状态一致。
11. 关键事件写入失败时任务安全失败。
12. 流式 delta 不会大量写入规范事件。

本阶段不要新增事件查询 API，不实现服务重启修复。

最终回复需用 fake 集成测试证明下一轮能够恢复上一轮完整工具链。
