# 阶段二：旧数据迁移与兼容投影

项目路径：`/Users/mac/Android-Agent`

请直接实施本阶段修改。阶段一的 `conversation_turns`、`conversation_events` 和事件存储服务应已存在。

## 通用要求

- 只完成旧数据迁移与旧格式投影，不接入模型循环。
- 保留现有未提交修改，不修改密钥，不调用真实模型或网络。
- 现有 Conversation API 和桌面端展示必须兼容。
- 完成后运行全部 Python 测试。

开始前阅读：

- `agent/database.py`
- `agent/conversation_events.py`
- `agent/jobs.py`
- `tests/test_mvp_core.py`
- `tests/test_conversation_events.py`

## 迁移 conversations.turns_json

数据库初始化时检查每个 Conversation：

- 已存在规范事件时不得重复迁移。
- 没有规范事件但 `turns_json` 有内容时执行迁移。
- 迁移必须幂等。

每个旧 Turn 生成：

1. `conversation_turn`
2. `user_message`
3. `assistant_message`
4. `changes`（存在 `changed_files` 时）
5. `turn_completed`

`user_message` payload：

```json
{
  "message_id": "稳定ID",
  "content": [{"type": "text", "text": "原用户输入"}],
  "source": "legacy",
  "legacy_imported": true
}
```

`assistant_message` payload：

```json
{
  "message_id": "稳定ID",
  "text_blocks": [
    {"block_index": 0, "type": "text", "text": "原回复"}
  ],
  "finish_reason": "stop",
  "is_final": true,
  "streamed": false,
  "legacy_imported": true
}
```

要求：

- 使用稳定 `event_key`，重复启动不会重复导入。
- 尽量保留旧 `ts`。
- 保留 `turns_json` 字段和旧表，不做破坏性迁移。
- 损坏的单个 Conversation 不应阻止服务启动，应留下诊断信息。
- 不能破坏 `project_sessions` 的既有迁移逻辑。

## 旧格式 turns 投影

从规范事件投影现有格式：

```json
{
  "user": "...",
  "assistant": "...",
  "changed_files": [],
  "ts": 0
}
```

规则：

- 一个 `conversation_turn` 投影为一个旧格式 Turn。
- `user` 来自该 Turn 的 `user_message`。
- `assistant` 优先取 `is_final=true` 的 `assistant_message`，否则取最后一条。
- `changed_files` 来自 `changes` 事件。
- 中间工具调用和模型响应不能变成额外的旧格式 Turn。
- `turn_count` 取投影后的数量。

调整：

- `get_conversation()`
- `get_conversation_turns()`
- `append_conversation_turn()`

要求：

- `get_conversation()` 继续返回 `turns` 和 `turn_count`。
- 新数据以规范事件为权威来源。
- `append_conversation_turn()` 保留兼容入口，但内部改为创建规范 Turn/Event。

## 测试

至少覆盖：

1. `turns_json` 正确迁移。
2. 迁移重复执行仍幂等。
3. 已有事件的 Conversation 不重复迁移。
4. `user`、`assistant`、`changed_files`、`ts` 得到保留。
5. `get_conversation()` 返回兼容格式。
6. `turn_count` 正确。
7. `append_conversation_turn()` 实际写入规范事件。
8. `project_sessions` 旧迁移测试继续通过。
9. 单条损坏旧数据不会导致全库启动失败。

本阶段不要修改 `agent/loop.py`，不要改变模型上下文，不新增事件查询 API，不实现中断修复。

最终回复需说明迁移策略、幂等方式、兼容性和测试结果。
